{
  "name": "bookshop",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "dependencies": {
    "@sap/cds": "^9",
    "cds-softdelete-plugin": "file:../../.",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@cap-js/cds-test": "^0"
  },
  "scripts": {
    "start": "cds-serve",
    "test": "node --test"
  },
  "private": true,
  "cds": {
    "logs": {
      "levels": {
        "deploy": "silent",
        "odata": "silent",
        "cds": "silent",
        "cds.serve": "silent"
      }
    }
  }
}
